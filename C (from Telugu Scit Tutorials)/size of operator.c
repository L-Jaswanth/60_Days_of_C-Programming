#include <stdio.h>

int main()
{
     int a=32341213;
     float b;
     char c;
     double d;
     float e=545674.548;
     
     printf("\nThe memory required for a:%d bytes",sizeof(a));
     printf("\nThe memory required for b:%d bytes",sizeof(b));
     printf("\nThe memory required for c:%d bytes",sizeof(c));
     printf("\nThe memory required for d:%d bytes",sizeof(d));
     printf("\nThe memory required for e:%d bytes",sizeof(e));
     
}
